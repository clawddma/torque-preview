preview-*.png
servidor/datos/
*.db
*.db-journal

# Credenciales de la puerta. El repositorio es PUBLICO: esto no se sube nunca.
servidor/usuarios.json

# Copias con fecha: son respaldos manuales, no versiones.
*.pre-*-[0-9]*
